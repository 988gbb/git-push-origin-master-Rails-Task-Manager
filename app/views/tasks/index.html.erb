<h2>Tasks</h2>
<ul>
<% @tasks.each do |task| %>

  <li><%= task.title %>
    <%= link_to "See task details", task_path(task) %>
    <%= link_to "Delete task", task_path(task), method: :delete %>
  </li>


<% end %>
</ul>

<%= link_to "Add new", tasks_new_path %>
